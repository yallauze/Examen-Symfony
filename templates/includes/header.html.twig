
<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top px-5">

    <a class="navbar-brand mr-2" href="{{ path('project') }}"> 
        <div class="d-flex flex-column align-items-center">
            <img src="{{ asset('data/img/logo.png') }}" alt="Logo" class="logoClass">
            <span class="text-bolder">PROJECT HUB</span>
        </div>
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mynavbar">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse d-flex justify-content-around ml-5 col-10" id="mynavbar">
       <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link {% if app.request.get('_route') == 'project' %} active {% endif %}" href="{{ path('project') }}">PROJECTS / HomePage</a>
            </li>
        </ul>
         {% if app.user is null %}
            <a class="btn btn-outline-success" href="{{ path('app_login') }}">Log In</a>
        {% else %}
            <div class="btn-group">
                <button type="button" class="btn btn-outline-info dropdown-toggle" data-toggle="dropdown"> Welcome: {{ app.user.getUsername() }} </button>
                <div class="dropdown-menu">
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Log Out</a>
                </div>
            </div>
        {% endif %}

    </div>
</nav>